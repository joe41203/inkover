{
  "name": "inkover",
  "private": true,
  "version": "0.1.2",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "pnpm build && node tests/e2e/overlay.e2e.mjs",
    "e2e:headed": "pnpm build && E2E_HEADED=1 node tests/e2e/overlay.e2e.mjs",
    "postinstall": "wxt prepare",
    "latency": "pnpm build && node tests/e2e/latency.e2e.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "web-ext": "^10.5.0",
    "wxt": "^0.21.2"
  },
  "dependencies": {
    "perfect-freehand": "^1.2.2"
  }
}
